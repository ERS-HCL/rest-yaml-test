info.build.artifact=@project.artifactId@
info.build.name=@project.name@
info.build.description=@project.description@
info.build.version=@project.version@

server.port=443
server.baseURI=https://httpbin.org/
testFiles=test-1.yaml,test-2.yaml
# you can add more than one certificates to save future modifications
certificates=certificates/httpbin.cer,certificates/alice.crt, certificates/bob.crt, certificates/carol.crt
logDebug=false

# uncomment to see how tagging feature works.
#groupTags=bodyAssert 
#testTags=jsonUnit,pipe